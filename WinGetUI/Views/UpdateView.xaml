<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinGetUI.Views.UpdateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WinGetUI.Views"
    Loaded="UserControl_Loaded">

    <StackPanel Margin="20">
        <TextBlock Text="Update Programs" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click" Padding="10,5" Margin="0,0,10,0"/>
            <Button x:Name="SelectAllButton" Content="Select All" Click="SelectAllButton_Click" Padding="10,5" Margin="0,0,5,0"/>
            <Button x:Name="DeselectAllButton" Content="Deselect All" Click="DeselectAllButton_Click" Padding="10,5" Margin="0,0,10,0"/>
            <TextBlock x:Name="SelectionStatus" Text="Selected: 0" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="DarkBlue" FontWeight="Bold"/>
            <Separator Width="1" Margin="20,0,20,0"/>
            <TextBlock Text="View:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
            <RadioButton x:Name="UpdatableViewRadio" Content="Updatable Only" IsChecked="True" VerticalAlignment="Center" Margin="0,0,15,0" GroupName="ViewMode" Checked="ViewModeRadio_Checked"/>
            <RadioButton x:Name="AllInstalledViewRadio" Content="All Installed" VerticalAlignment="Center" GroupName="ViewMode" Checked="ViewModeRadio_Checked"/>
            <Separator Width="1" Margin="20,0,20,0"/>
            <TextBlock Text="Mode:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
            <RadioButton x:Name="NormalModeRadio" Content="Normal" IsChecked="True" VerticalAlignment="Center" Margin="0,0,15,0" GroupName="UpdateMode"/>
            <RadioButton x:Name="SilentModeRadio" Content="Silent" VerticalAlignment="Center" GroupName="UpdateMode"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Text="Ready" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="DarkGreen"/>

        <DataGrid x:Name="PackagesGrid" Height="350" Background="White" BorderBrush="LightGray" BorderThickness="1" 
                  Margin="0,0,0,10" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  ItemsSource="{Binding UpdatablePackages}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected}" Width="60"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="200" IsReadOnly="True"/>
                <DataGridTextColumn x:Name="CurrentVersionColumn" Header="Current Version" Binding="{Binding Version}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn x:Name="AvailableVersionColumn" Header="Available Version" Binding="{Binding AvailableVersion}" Width="120" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
            <Button x:Name="UpdateButton" Content="Update Selected Packages" Click="UpdateButton_Click" Padding="15,8" Background="Blue" Foreground="White" Margin="0,0,10,0"/>
            <Button x:Name="UninstallButton" Content="Uninstall Selected Packages" Click="UninstallButton_Click" Padding="15,8" Background="Red" Foreground="White"/>
        </StackPanel>
    </StackPanel>
</UserControl>
