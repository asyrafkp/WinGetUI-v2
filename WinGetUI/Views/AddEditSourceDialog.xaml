<Window x:Class="WinGetUI.Views.AddEditSourceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add/Edit Package Source" Width="550"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        SizeToContent="Height"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Source Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Source Name *" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="NameTextBox" Height="32" Padding="8,0"/>
        </StackPanel>

        <!-- Source URL/Argument -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Source URL/Argument *" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="UrlTextBox" Height="32" Padding="8,0"/>
        </StackPanel>

        <!-- Source Type -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Source Type (Optional)" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="TypeComboBox" Height="32">
                <ComboBoxItem Content="" IsSelected="True"/>
                <ComboBoxItem Content="Microsoft.PreIndexed.Package"/>
                <ComboBoxItem Content="Microsoft.Rest"/>
            </ComboBox>
        </StackPanel>

        <!-- Help Text -->
        <TextBlock Grid.Row="3" 
                   Text="* Required fields. Administrator privileges may be required."
                   Foreground="{DynamicResource DimTextBrush}"
                   FontSize="11"
                   Margin="0,0,0,10"/>

        <!-- Quick Add Section -->
        <Border Grid.Row="4" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10" Background="{DynamicResource SecondaryBrush}" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="Quick Add Popular Sources" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button x:Name="ChocolateyButton" Content="🍫 Chocolatey" 
                            Width="120" Height="28" Margin="0,0,5,0"
                            Click="ChocolateyButton_Click"
                            Background="#80B5E3" Foreground="White" BorderThickness="0"
                            ToolTip="Add Chocolatey community repository"/>
                    <Button x:Name="WingetCommunityButton" Content="📦 Winget Community" 
                            Width="150" Height="28" Margin="0,0,5,0"
                            Click="WingetCommunityButton_Click"
                            Background="#0078D4" Foreground="White" BorderThickness="0"
                            ToolTip="Add winget-pkgs community repository"/>
                    <Button x:Name="ChocolateyGitHubButton" Content="🐙 Choc GitHub" 
                            Width="130" Height="28"
                            Click="ChocolateyGitHubButton_Click"
                            Background="#333333" Foreground="White" BorderThickness="0"
                            ToolTip="Add Chocolatey Community GitHub packages repository"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" 
                    Width="100" Height="32" Margin="0,0,10,0"
                    Click="OkButton_Click"
                    Background="{DynamicResource SuccessStatusBrush}" Foreground="White" BorderThickness="0"/>
            <Button x:Name="CancelButton" Content="Cancel" 
                    Width="100" Height="32"
                    Click="CancelButton_Click"
                    Background="{DynamicResource SecondaryBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
        </StackPanel>
    </Grid>
</Window>
