<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinGetUI.Views.SourcesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WinGetUI.Views"
    Loaded="UserControl_Loaded">

    <StackPanel Margin="20">
        <TextBlock Text="Package Sources" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click" Padding="10,5" Margin="0,0,10,0"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Text="Ready" TextWrapping="Wrap" Margin="0,0,0,10" Foreground="DarkGreen"/>

        <DataGrid x:Name="SourcesGrid" Height="400" Background="White" BorderBrush="LightGray" BorderThickness="1" 
                  Margin="0,0,0,10" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  ItemsSource="{Binding PackageSources}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150" IsReadOnly="True"/>
                <DataGridTextColumn Header="Argument" Binding="{Binding Argument}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Data" Binding="{Binding Data}" Width="200" IsReadOnly="True"/>
                <DataGridCheckBoxColumn Header="Default" Binding="{Binding IsDefault}" Width="80" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>

